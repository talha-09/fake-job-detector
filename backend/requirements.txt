# Production dependencies (API serving)
# Versions pinned to match local training environment where .pkl models were saved.
# Do NOT downgrade these — the .pkl files were serialised with these exact versions.
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
scikit-learn==1.8.0
xgboost==3.2.0
scipy==1.17.1
numpy==2.4.6
nltk==3.9.4
joblib==1.5.3
python-multipart==0.0.6

# Training-only (not required on production server)
# pandas==2.2.0
# matplotlib==3.9.0
# seaborn==0.13.2
# imbalanced-learn==0.12.3
